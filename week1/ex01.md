👉 Nói bằng tiếng Việt đời thường:

```
Chương trình này gọi Gemini (Google GenAI), gửi câu hỏi “GenAI là gì?” và in câu trả lời ra màn hình
```

# 🧱 GIẢI THÍCH CHI TIẾT TỪ TRÊN XUỐNG

## 1️⃣ Import thư viện

```
from google import genai
import os
```

🔹 `from google import genai`

- Import SDK chính thức của Google GenAI
- Cho phép:
    - gọi model Gemini
    - gửi prompt
    - nhận response

👉 Không có dòng này → không dùng được Gemini

🔹 `import os`

- Dùng để:
    - đọc biến môi trường
    - lấy API key an toàn

👉 Đây là best practice, không hard-code key trong file

## 2️⃣ Định nghĩa hàm main()

```
def main():
```

- Gói toàn bộ logic vào 1 hàm
- Giúp:
    - code gọn
    - dễ mở rộng
    - tránh chạy nhầm khi import file

## 3️⃣ Kiểm tra API key (CỰC KỲ QUAN TRỌNG)

```
if "GEMINI_API_KEY" not in os.environ:
    raise RuntimeError("Chưa set biến môi trường GEMINI_API_KEY")
```

### 🧠 Dòng này làm gì?

- Kiểm tra:
    - Máy tính đã có API key chưa?
- Nếu chưa có:
    - chương trình dừng ngay
    - báo lỗi rõ ràng

👉 Giúp bạn không phải debug lỗi mơ hồ sau này

## 4️⃣ Tạo client Gemini

```
client = genai.Client(
    api_key=os.environ["GEMINI_API_KEY"]
)
```

### 🧠 Hiểu đơn giản:

- Client = cửa giao tiếp với Gemini
- api_key = chìa khóa để dùng dịch vụ

👉 Mỗi lần gọi model → đi qua client

### ❓ Vì sao dùng os.environ["GEMINI_API_KEY"]?

- API key được lưu ngoài code
- Không bị:
    - lộ key khi push GitHub
    - share nhầm

👉 Cách làm chuyên nghiệp

## 5️⃣ Gọi model sinh nội dung (QUAN TRỌNG NHẤT)

```
response = client.models.generate_content(
    model="gemini-flash-lite-latest",
    contents="GenAI là gì?"
)
```

### 🧠 Tách từng phần:

🔹 `client.models.generate_content(...)`

- Gọi API tạo nội dung
- Đây là hàm cốt lõi của GenAI

🔹 `model="gemini-flash-lite-latest"`

- Model:
    - nhanh
    - rẻ
    - quota thoáng
- Rất phù hợp người mới học

👉 "latest" = luôn dùng bản ổn định mới nhất

🔹 `contents="GenAI là gì?"`

- Đây là prompt
- Nội dung bạn gửi cho LLM

👉 Có thể thay bằng:

```
contents="Giải thích LLM cho học sinh cấp 3"
```

## 6️⃣ In kết quả ra màn hình

```
print(response.text)
```

### 🧠 response là gì?

- Object chứa:
    - text
    - metadata
    - token usage

👉 .text là:

```
Câu trả lời LLM sinh ra
```

## 7️⃣ Entry point của Python

```
if __name__ == "__main__":
    main()
```

### 🧠 Ý nghĩa:

- Chỉ chạy `main()` khi:
    - bạn chạy file trực tiếp

👉 Không chạy khi file bị import vào file khác

# 🎯 TÓM TẮT LOGIC TOÀN CHƯƠNG TRÌNH

```
1. Kiểm tra có API key chưa
2. Tạo client Gemini
3. Gửi prompt "GenAI là gì?"
4. Nhận câu trả lời
5. In ra màn hình
```

# 🧠 ĐÂY LÀ MỨC KIẾN THỨC GÌ?

👉 Bạn vừa học xong:

✔️ Cách gọi LLM

✔️ Prompt cơ bản

✔️ Cách quản lý API key

✔️ Model selection

✔️ Debug quota / auth

➡️ Tương đương “Hello World” của GenAI

# 🚀 BƯỚC TIẾP THEO (RẤT TỰ NHIÊN)

Giờ bạn có thể học tiếp:

1️⃣ Chat nhiều lượt (conversation history)

2️⃣ Prompt engineering (system / role)

3️⃣ Streaming output (in từng token)

4️⃣ Mini chatbot CLI
